int var = 0;

int main() {
	// varlist = add the variable in a global list (mostly for scripting and debugging)
	// int = type of the variable
	// x = name of the variable
	// 0 = initial value of the variable
	/*varlist*/ int x = 0;

	// return = end function
	// 0 = function "exit code" (can be anything, but must be the same type like the function)
	return 0;
}